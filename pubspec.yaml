name: flutter_notebook_19th_story
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.17.0-227.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.4
  flutter_riverpod: ^2.0.0-dev.5
  freezed_annotation: any

  url_launcher: ^6.0.20

dev_dependencies:
  flutter_test:
    sdk: flutter

  # preview
  flutter_lints: ^2.0.0
  build_runner: any
  json_serializable: ^6.1.5
  freezed:  any

flutter:
  uses-material-design: true
