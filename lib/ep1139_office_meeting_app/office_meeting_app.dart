import 'package:flutter/material.dart';

class OfficeMeetingApp extends StatelessWidget {
  const OfficeMeetingApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: OfficeMainPage(),
    );
  }
}
